import React from "react";
import { HeaderContainer } from "../StyledComponents/HeaderElements";
import Navbar from "./Navbar";

function Header() {
  return (
    <HeaderContainer>
      <Navbar></Navbar>
    </HeaderContainer>
  );
}

export default Header;
